/*
Deployment script for recorder

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
/*
:setvar DatabaseName "recorder"
:setvar DefaultFilePrefix "recorder"
:setvar DefaultDataPath ""
:setvar DefaultLogPath ""
*/

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
PRINT N'Altering [dbo].[ClientSettings]...';


GO
ALTER TABLE [dbo].[ClientSettings]
    ADD [MsProjectAddress] NVARCHAR (200) NULL;


GO
PRINT N'Altering [dbo].[GetIdForCollectedKey]...';


GO
-- =============================================
-- Author: Zoltan Torok
-- =============================================
ALTER PROCEDURE [dbo].[GetIdForCollectedKey]
	(
	@key nvarchar(4000),
	@id int OUTPUT
	)
AS
	SET NOCOUNT ON
	SET XACT_ABORT ON

	IF @key IS NULL
	BEGIN
		RAISERROR('@@key cannot be NULL', 16, 1)
		RETURN
	END

declare @hashCode int
SET @hashCode = CHECKSUM(@key)

--optimize for common case, we don't want to put index on (hash,value) or handle deadlocks so we won't use UPDLOCK
SET @id = (
	SELECT TOP 1 [Id]
	  FROM [dbo].[CollectedKeyLookup]
	 WHERE [HashCode] = @hashCode
	   AND [Key] = @key
	)

IF @id IS NOT NULL
	RETURN

BEGIN TRAN

SET @id = (
	SELECT TOP 1 [Id]
	  FROM [dbo].[CollectedKeyLookup] WITH (TABLOCKX, HOLDLOCK)
	 WHERE [HashCode] = @hashCode
	   AND [Key] = @key
	)

IF @id IS NULL
BEGIN
	INSERT INTO [dbo].[CollectedKeyLookup]
			   ([HashCode]
			   ,[Key])
		 VALUES
			   (@hashCode
			   ,@key)

	SET @id = SCOPE_IDENTITY()
END

COMMIT TRAN

	RETURN
GO
PRINT N'Altering [dbo].[GetIdForCollectedValue]...';


GO
-- =============================================
-- Author: Zoltan Torok
-- =============================================
ALTER PROCEDURE [dbo].[GetIdForCollectedValue]
	(
	@value nvarchar(4000),
	@id int OUTPUT
	)
AS
	SET NOCOUNT ON
	SET XACT_ABORT ON

	IF @value IS NULL
	BEGIN
		RAISERROR('@value cannot be NULL', 16, 1)
		RETURN
	END

declare @hashCode int
SET @hashCode = CHECKSUM(@value)

--optimize for common case, we don't want to put index on (hash,value) or handle deadlocks so we won't use UPDLOCK
SET @id = (
	SELECT TOP 1 [Id]
	  FROM [dbo].[CollectedValueLookup]
	 WHERE [HashCode] = @hashCode
	   AND [Value] = @value
	)

IF @id IS NOT NULL
	RETURN

BEGIN TRAN

SET @id = (
	SELECT TOP 1 [Id]
	  FROM [dbo].[CollectedValueLookup] WITH (TABLOCKX, HOLDLOCK)
	 WHERE [HashCode] = @hashCode
	   AND [Value] = @value
	)

IF @id IS NULL
BEGIN
	INSERT INTO [dbo].[CollectedValueLookup]
			   ([HashCode]
			   ,[Value])
		 VALUES
			   (@hashCode
			   ,@value)

	SET @id = SCOPE_IDENTITY()
END

COMMIT TRAN

	RETURN
GO
PRINT N'Altering [dbo].[GetSchemaVersion]...';


GO
-- Don't modify this file, it's used in the build process!
ALTER PROCEDURE [dbo].[GetSchemaVersion]
AS RETURN 15
GO
PRINT N'Refreshing [dbo].[InsertCollectedItem]...';


GO
EXECUTE sp_refreshsqlmodule N'[dbo].[InsertCollectedItem]';


GO
PRINT N'Update complete.';


GO
