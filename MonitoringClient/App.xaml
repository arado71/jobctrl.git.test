<Application xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Controls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Data.Input"
             x:Class="MonitoringClient.App"
             >
    <Application.Resources>
        <Style x:Key="TitleText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#FF14517B"/>
            <Setter Property="Margin" Value="0,10,0,0"/>
        </Style>
        <Style x:Key="Title2Text" TargetType="TextBlock" BasedOn="{StaticResource TitleText}">
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style x:Key="Title2TextNoMargin" TargetType="TextBlock" BasedOn="{StaticResource Title2Text}">
            <Setter Property="Margin" Value="0"/>
        </Style>

        <Style x:Key="ImageLabel" TargetType="Controls:Label">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="Foreground" Value="#FFFFFFFF"/>
            <Setter Property="Background" Value="#99000000"/>
            <Setter Property="BorderBrush" Value="#770000BB"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="ImageLabelMargin" TargetType="Controls:Label" BasedOn="{StaticResource ImageLabel}">
            <Setter Property="Margin" Value="5,20,5,0"/>
        </Style>
    </Application.Resources>
</Application>
