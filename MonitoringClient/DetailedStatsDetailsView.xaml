<UserControl x:Class="MonitoringClient.DetailedStatsDetailsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="480" d:DesignWidth="640"
    xmlns:my="clr-namespace:MonitoringClient.ActivityMonitoringServiceReference"
    xmlns:my1="clr-namespace:MonitoringClient" xmlns:my2="clr-namespace:MonitoringClient.Converters" xmlns:System="clr-namespace:System;assembly=mscorlib">
    <UserControl.Resources>
        <my2:ByteArrayToImageConverter x:Key="ByteArrayToImageConverter" />
        <Style x:Key="HoverZoomControlStyle" TargetType="Control">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Control">
                        <Viewbox x:Name="mainContent" Stretch="None" RenderTransformOrigin="0.5,0.5" Canvas.ZIndex="0" MouseEnter="mainContent_MouseEnter">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="FocusStates">
                                    <VisualState x:Name="Unfocused"/>
                                    <VisualState x:Name="Focused"/>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualStateGroup.Transitions>
                                        <VisualTransition GeneratedDuration="0:0:0.3">
                                            <VisualTransition.GeneratedEasingFunction>
                                                <CubicEase EasingMode="EaseInOut"/>
                                            </VisualTransition.GeneratedEasingFunction>
                                        </VisualTransition>
                                        <VisualTransition From="MouseOver" GeneratedDuration="0:0:0" To="Pressed">
                                            <Storyboard>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="mainContent"  Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)">
                                                    <SplineDoubleKeyFrame KeyTime="00:00:00" Value="1.8"/>
                                                </DoubleAnimationUsingKeyFrames>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="mainContent" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)">
                                                    <SplineDoubleKeyFrame KeyTime="00:00:00" Value="1.8"/>
                                                </DoubleAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualTransition>
                                    </VisualStateGroup.Transitions>
                                    <VisualState x:Name="Normal">
                                        <!--<Storyboard>
                                            <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" AutoReverse="False" RepeatBehavior="1x" Storyboard.TargetName="mainContent" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)">
                                                <SplineDoubleKeyFrame KeyTime="00:00:00.3" Value="1"/>
                                            </DoubleAnimationUsingKeyFrames>
                                            <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" AutoReverse="False" RepeatBehavior="1x" Storyboard.TargetName="mainContent" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)">
                                                <SplineDoubleKeyFrame KeyTime="00:00:00.3" Value="1"/>
                                            </DoubleAnimationUsingKeyFrames>
                                        </Storyboard>-->
                                    </VisualState>
                                    <VisualState x:Name="Pressed"/>
                                    <VisualState x:Name="Disabled"/>
                                    <VisualState x:Name="MouseOver">
                                        <Storyboard>
                                            <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" AutoReverse="False" RepeatBehavior="1x" Storyboard.TargetName="mainContent"  Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)">
                                                <SplineDoubleKeyFrame KeyTime="00:00:00.3" Value="1.8"/>
                                            </DoubleAnimationUsingKeyFrames>
                                            <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" AutoReverse="False" RepeatBehavior="1x" Storyboard.TargetName="mainContent" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)">
                                                <SplineDoubleKeyFrame KeyTime="00:00:00.3" Value="1.8"/>
                                            </DoubleAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Viewbox.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform ScaleX="1" ScaleY="1" />
                                </TransformGroup>
                            </Viewbox.RenderTransform>
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Viewbox>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <my2:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <my2:StatusToBrushConverter x:Key="StatusToBrushConverter" />
    </UserControl.Resources>
    <Grid x:Name="LayoutRoot" Background="White" d:DataContext="{d:DesignInstance Type=my:DetailedUserStats}" MouseEnter="LayoutRoot_MouseEnter" MouseLeave="LayoutRoot_MouseLeave">
        <!--<Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <my1:BriefStatsDetailsView Grid.Column="0" Grid.Row="0" x:Name="BriefStatsDetailsView" DataContext="{Binding}"/>
        <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding Path=UserName}" />-->
        <Border BorderBrush="#FF000000" CornerRadius="0" BorderThickness="1">
            <Viewbox Width="320" Height="256">
                <Button Style="{StaticResource HoverZoomControlStyle}">
                    <Grid Width="640" Height="512" Background="#94FFFFFF">
                        <my1:DetailedStatsGrid Canvas.ZIndex="2"/>
                        <Rectangle StrokeThickness="0" Fill="{Binding Path=Status, Converter={StaticResource StatusToBrushConverter}}" Opacity="0.4" Canvas.ZIndex="1"/>
                        <Viewbox>
                            <ItemsControl ItemsSource="{Binding Path=ComputerStatsByCompId.Values}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <!--<TextBlock Canvas.ZIndex="1" Text="{Binding Path=ComputerId}" FontSize="15"/>-->
                                            <Rectangle Canvas.ZIndex="1" StrokeThickness="0" Fill="Black" Opacity="0.4" Visibility="{Binding Path=IsOnline, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Negate}" />
                                            <ItemsControl ItemsSource="{Binding Path=RecentComputerActivity.LastScreenShots}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <StackPanel Orientation="Horizontal"  />
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Image Source="{Binding Path=Data, Converter={StaticResource ByteArrayToImageConverter}}" >
                                                        </Image>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Viewbox>
                    </Grid>
                </Button>
            </Viewbox>
        </Border>
    </Grid>
</UserControl>
